# Professor in a Box — Inference Server Dependencies
# Install on EC2 with: pip install -r requirements.txt

# Core inference stack
transformers==4.44.0
peft>=0.12.0
accelerate>=0.30.0
bitsandbytes>=0.43.0

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# HuggingFace Hub (for adapter download)
huggingface-hub>=0.23.0

# AWS CloudWatch logging
boto3>=1.34.0

# PyTorch — already on Deep Learning AMI, but listed for reference
# torch>=2.3.0  (pre-installed on AWS Deep Learning AMI — do not reinstall)

# Pydantic (FastAPI dependency, usually auto-installed)
pydantic>=2.0.0
